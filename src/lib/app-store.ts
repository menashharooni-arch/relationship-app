// Where the "Download on the App Store" buttons point. Set
// NEXT_PUBLIC_APP_STORE_URL to the real listing once the app is published;
// until then this opens the App Store so the button is never a dead link.
export const APP_STORE_URL =
  process.env.NEXT_PUBLIC_APP_STORE_URL || "https://apps.apple.com/";
